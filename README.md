# spacebox-prettier-config
This is to share the same prettier auto format accross the Spacebox's team.

## Usage
Install:

`$ npm install --dev @wahidshadab/spacebox-prettier-config`

Edit package.json:
```
{
  // ...
  "prettier": "@wahidshadab/spacebox-prettier-config"
}
```
